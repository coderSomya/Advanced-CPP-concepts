struct person{
    int a; //public
};

class person{
    int a; //private
};


/*
structs are passive objects which are used to bundle public data and have little to no functions

classes are active type objects which have members interfaced with functions

classes can be inherited whereas structs can't

null values are possible in class
*/